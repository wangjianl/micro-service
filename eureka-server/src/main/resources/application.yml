spring:
  application:
    name: eureka-server
server:
  port: 7070


eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
       defaultZone: http://localhost:${server.port}/eureka/

security:
  user:
    name: name
    password: password