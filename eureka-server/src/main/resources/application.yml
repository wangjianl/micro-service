spring:
  application:
    name: eureka-server
server:
  port: 7070


eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
       defaultZone: http://127.0.0.1:${server.port}/eureka/

security:
  user:
    name: name
    password: password